---
modified: 2026-04-12T22:29:53+05:30
---
### ***~={purple}~={cyan}Nmap=~=~

```prompt:kali
nmap -p445 --script smb-enum-shares 192.168.177.129
```

we can see the **shares**
the users who has access to read and write, we can utilize that. Thats why we are **enumerating**.

### ~={cyan}***Msf Console** -  =~

`$ msfconsole`

```prompt:msf module:auxiliary/scanner/smb/smb_enumshares
set rhosts 192.168.177.129
show options
exploit
exit
```

**`set rhosts` *is used to define the **Remote Host(s)** that you want to target with a specific  module.*
### ~={cyan}***enum4linux** =~-

```prompt:kali
enum4linux -S 192.168.177.129 (-S sharelist)
```

```prompt:kali
enum4linux -G 192.168.177.129 (-G groups)
```

```prompt:kali
enum4linux -i 192.168.177.129  (-i get printer info.)
```
### ~={cyan}***smbclient** =~-

```prompt:kali
smbclient -L 192.168.177.129 -N  
```

```prompt:kali
smbclient //192.168.177.129/Public -N
```
### ~={cyan}***rpcclient** =~-

```sh
$ rpcclient -U "" -N 192.168.177.129

rpcclient $> enumdomgroups
```
